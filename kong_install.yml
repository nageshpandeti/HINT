---
- name: Kong API Gateway - Install and Test
  hosts: localhost
  connection: local
  become: true
  gather_facts: true

  vars:
    pg_user: kong
    pg_password: "KongSecret@2024"
    pg_db: kong
    pg_host: "127.0.0.1"
    pg_port: 5432

    kong_conf: /etc/kong/kong.conf
    kong_proxy_port: 8000
    kong_admin_port: 8001
    kong_admin_url: "http://localhost:8001"
    kong_proxy_url: "http://localhost:8000"

    mock_port: 9090
    svc_name: local-mock-service
    svc_url: "http://localhost:9090"
    route_path: /test-api
    test_api_key: "my-local-test-key-2024"

  pre_tasks:
    - name: Assert Ubuntu
      assert:
        that: ansible_distribution == "Ubuntu"
        fail_msg: "This playbook requires Ubuntu"

    - name: Kill apt locks and background jobs
      shell: |
        killall apt apt-get unattended-upgrades dpkg 2>/dev/null || true
        systemctl stop unattended-upgrades 2>/dev/null || true
        rm -f /var/lib/dpkg/lock /var/lib/dpkg/lock-frontend
        rm -f /var/lib/apt/lists/lock /var/cache/apt/archives/lock
        dpkg --configure -a || true
      changed_when: false

    - name: Apt update
      apt:
        update_cache: yes
      changed_when: false

  tasks:
    - name: Install prerequisites
      apt:
        name:
          - curl
          - gnupg
          - apt-transport-https
          - ca-certificates
          - lsb-release
          - python3-psycopg2
          - postgresql
          - postgresql-contrib
        state: present
        update_cache: yes

    - name: Start and enable PostgreSQL
      service:
        name: postgresql
        state: started
        enabled: true

    - name: Wait for PostgreSQL
      wait_for:
        host: 127.0.0.1
        port: 5432
        timeout: 30

    - name: Create Kong DB user
      shell: |
        su - postgres -c "psql -tAc \"SELECT 1 FROM pg_roles WHERE rolname='{{ pg_user }}'\"" | grep -q 1 || \
        su - postgres -c "psql -c \"CREATE USER {{ pg_user }} WITH PASSWORD '{{ pg_password }}' CREATEDB LOGIN;\""
      args:
        executable: /bin/bash
      changed_when: true

    - name: Create Kong database
      shell: |
        su - postgres -c "psql -tAc \"SELECT 1 FROM pg_database WHERE datname='{{ pg_db }}'\"" | grep -q 1 || \
        su - postgres -c "psql -c \"CREATE DATABASE {{ pg_db }} OWNER {{ pg_user }} ENCODING 'UTF8';\""
      args:
        executable: /bin/bash
      changed_when: true

    - name: Grant DB privileges
      shell: |
        su - postgres -c "psql -c \"GRANT ALL PRIVILEGES ON DATABASE {{ pg_db }} TO {{ pg_user }};\""
      changed_when: false

    - name: Remove any old mock process
      shell: |
        fuser -k {{ mock_port }}/tcp 2>/dev/null || true
        rm -f /tmp/mock_server.pid
      changed_when: false

    - name: Create local mock backend
      copy:
        dest: /tmp/mock_server.py
        mode: "0755"
        content: |
          #!/usr/bin/env python3
          from http.server import HTTPServer, BaseHTTPRequestHandler
          import json, datetime

          class Handler(BaseHTTPRequestHandler):
              def do_GET(self):
                  body = json.dumps({
                      "status": "ok",
                      "path": self.path,
                      "method": "GET",
                      "message": "Kong API Gateway mock backend",
                      "timestamp": datetime.datetime.utcnow().isoformat() + "Z"
                  }).encode("utf-8")
                  self.send_response(200)
                  self.send_header("Content-Type", "application/json")
                  self.send_header("Content-Length", str(len(body)))
                  self.end_headers()
                  self.wfile.write(body)

              def do_POST(self):
                  self.do_GET()

              def log_message(self, fmt, *args):
                  return

          if __name__ == "__main__":
              HTTPServer(("0.0.0.0", {{ mock_port }}), Handler).serve_forever()

    - name: Start mock backend
      shell: |
        nohup python3 /tmp/mock_server.py >/tmp/mock_server.log 2>&1 &
        echo $! >/tmp/mock_server.pid
      args:
        executable: /bin/bash
      changed_when: true

    - name: Wait for mock backend
      wait_for:
        host: 127.0.0.1
        port: "{{ mock_port }}"
        timeout: 15

    - name: Verify mock backend
      uri:
        url: "http://127.0.0.1:{{ mock_port }}/health"
        method: GET
        status_code: 200
        return_content: yes
      register: mock_check

    - name: Add Kong GPG key
      shell: |
        curl -fsSL https://packages.konghq.com/public/gateway-39/gpg.E7ECF02AC16C5DB8.key \
          | gpg --dearmor -o /usr/share/keyrings/kong-archive-keyring.gpg
      args:
        creates: /usr/share/keyrings/kong-archive-keyring.gpg

    - name: Add Kong repo
      copy:
        dest: /etc/apt/sources.list.d/kong-gateway-39.list
        content: |
          deb [signed-by=/usr/share/keyrings/kong-archive-keyring.gpg] https://packages.konghq.com/public/gateway-39/deb/ubuntu {{ ansible_distribution_release }} main

    - name: Update apt after Kong repo
      apt:
        update_cache: yes
      changed_when: false

    - name: Install Kong
      apt:
        name: kong
        state: present

    - name: Detect Kong binary
      command: which kong
      register: kong_bin
      changed_when: false

    - name: Show Kong binary
      debug:
        msg: "Using Kong binary: {{ kong_bin.stdout }}"

    - name: Copy default kong.conf
      copy:
        src: /etc/kong/kong.conf.default
        dest: "{{ kong_conf }}"
        remote_src: true
        force: false
        mode: "0644"

    - name: Configure kong.conf
      blockinfile:
        path: "{{ kong_conf }}"
        marker: "# {mark} ANSIBLE MANAGED BLOCK"
        block: |
          database = postgres
          pg_host = {{ pg_host }}
          pg_port = {{ pg_port }}
          pg_user = {{ pg_user }}
          pg_password = {{ pg_password }}
          pg_database = {{ pg_db }}
          proxy_listen = 0.0.0.0:{{ kong_proxy_port }}
          admin_listen = 0.0.0.0:{{ kong_admin_port }}

    - name: Check Kong config
      command: "{{ kong_bin.stdout }} check {{ kong_conf }}"
      register: kong_check
      changed_when: false

    - name: Bootstrap Kong DB
      command: "{{ kong_bin.stdout }} migrations bootstrap -c {{ kong_conf }}"
      register: mig
      failed_when: mig.rc != 0 and 'already bootstrapped' not in (mig.stderr | default(''))
      changed_when: "'already bootstrapped' not in (mig.stderr | default(''))"

    - name: Create Kong systemd unit
      copy:
        dest: /lib/systemd/system/kong.service
        mode: "0644"
        content: |
          [Unit]
          Description=Kong API Gateway
          After=network.target postgresql.service
          Wants=postgresql.service

          [Service]
          User=root
          Group=root
          Type=forking
          ExecStartPre={{ kong_bin.stdout }} prepare -p /usr/local/kong -c {{ kong_conf }}
          ExecStart={{ kong_bin.stdout }} start -c {{ kong_conf }}
          ExecReload={{ kong_bin.stdout }} reload -c {{ kong_conf }}
          ExecStop={{ kong_bin.stdout }} stop -c {{ kong_conf }}
          Restart=on-failure
          RestartSec=5s

          [Install]
          WantedBy=multi-user.target

    - name: Reload systemd
      systemd:
        daemon_reload: true

    - name: Start and enable Kong
      systemd:
        name: kong
        state: started
        enabled: true

    - name: Wait for Kong Admin API
      wait_for:
        host: 127.0.0.1
        port: "{{ kong_admin_port }}"
        timeout: 60

    - name: Wait for Kong Proxy
      wait_for:
        host: 127.0.0.1
        port: "{{ kong_proxy_port }}"
        timeout: 60

    - name: Check Admin API health
      uri:
        url: "{{ kong_admin_url }}"
        method: GET
        status_code: 200
        return_content: yes
      register: health

    - name: Delete old route
      uri:
        url: "{{ kong_admin_url }}/routes/test-route"
        method: DELETE
        status_code: [204, 404]

    - name: Delete old service
      uri:
        url: "{{ kong_admin_url }}/services/{{ svc_name }}"
        method: DELETE
        status_code: [204, 404]

    - name: Create service
      uri:
        url: "{{ kong_admin_url }}/services"
        method: POST
        body_format: json
        body:
          name: "{{ svc_name }}"
          url: "{{ svc_url }}"
        status_code: [200, 201]
      register: svc_result

    - name: Create route
      uri:
        url: "{{ kong_admin_url }}/services/{{ svc_name }}/routes"
        method: POST
        body_format: json
        body:
          name: test-route
          paths:
            - "{{ route_path }}"
          strip_path: true
        status_code: [200, 201]
      register: route_result

    - name: Add rate limiting plugin
      uri:
        url: "{{ kong_admin_url }}/services/{{ svc_name }}/plugins"
        method: POST
        body_format: json
        body:
          name: rate-limiting
          config:
            minute: 10
            policy: local
        status_code: [200, 201, 409]

    - name: Add key-auth plugin
      uri:
        url: "{{ kong_admin_url }}/services/{{ svc_name }}/plugins"
        method: POST
        body_format: json
        body:
          name: key-auth
          config:
            key_names:
              - apikey
        status_code: [200, 201, 409]

    - name: Delete old consumer
      uri:
        url: "{{ kong_admin_url }}/consumers/test-user"
        method: DELETE
        status_code: [204, 404]

    - name: Create consumer
      uri:
        url: "{{ kong_admin_url }}/consumers"
        method: POST
        body_format: json
        body:
          username: test-user
        status_code: [200, 201]

    - name: Assign API key
      uri:
        url: "{{ kong_admin_url }}/consumers/test-user/key-auth"
        method: POST
        body_format: json
        body:
          key: "{{ test_api_key }}"
        status_code: [200, 201]

    - name: E2E authenticated request
      uri:
        url: "{{ kong_proxy_url }}{{ route_path }}/hello"
        method: GET
        headers:
          apikey: "{{ test_api_key }}"
        status_code: 200
        return_content: yes
      register: e2e

    - name: E2E unauthenticated request
      uri:
        url: "{{ kong_proxy_url }}{{ route_path }}/hello"
        method: GET
        status_code: 401
      register: unauth

    - name: Summary
      debug:
        msg:
          - "Kong version: {{ health.json.version }}"
          - "Proxy URL: {{ kong_proxy_url }}"
          - "Admin URL: {{ kong_admin_url }}"
          - "Mock backend: http://127.0.0.1:{{ mock_port }}"
          - "Route: {{ route_path }}"
          - "Authenticated test: HTTP {{ e2e.status }}"
          - "Unauthenticated test: HTTP {{ unauth.status }}"
          - "API key: {{ test_api_key }}"
